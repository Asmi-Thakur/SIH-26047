# Frontend environment (Vite). Copy to .env to override defaults.
# Only non-secret, public configuration lives here — never API keys.
# The backend base URL must be reachable from the browser.
VITE_API_BASE_URL=http://localhost:8000